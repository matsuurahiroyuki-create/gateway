FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (none needed for these simple servers)
RUN npm install --production 2>/dev/null || true

# Copy source
COPY . .

# Run as non-root
USER node

# Start server
CMD ["node", "server.js"]
